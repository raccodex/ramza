<?php
$rac_theme_profiles = array(
    'Ramza New' => array(
        'summary' => 'The next Ramza experience is being prepared and is locked until the upcoming release.',
        'label' => 'Coming soon',
        'locked' => true,
    ),
    'Ramza Light' => array(
        'summary' => 'The default Ramza front-end theme for fresh installs and current supported sites.',
        'label' => 'Default',
        'locked' => false,
    ),
);
?>
<div class="container-fluid rac-theme-page">
    <div class="rac-theme-head">
        <div>
            <h3>Ramza themes</h3>
            <p>Manage the built-in Ramza themes. Only supported Ramza themes are shown in this panel.</p>
            <nav aria-label="breadcrumb">
                <ol class="breadcrumb">
                    <li class="breadcrumb-item">
                        <a href="#">Home</a>
                    </li>
                    <li class="breadcrumb-item">
                        <a href="#">Design</a>
                    </li>
                    <li class="breadcrumb-item active" aria-current="page">Themes</li>
                </ol>
            </nav>
        </div>
    </div>

    <div class="pro-settings-alert"></div>
    <form action="#" method="post" class="setting-themes-container">
        <div class="row rac-theme-grid">
            <?php
            foreach (Wo_GetThemes() as $theme_url) {
                if (!file_exists($theme_url . '/info.php')) {
                    continue;
                }

                $themeName = '';
                $themeFolder = '';
                $themeAuthor = 'Ramza';
                $themeAuthorUrl = 'mailto:ramzasocial@gmail.com';
                $themeVirsion = '1.0';
                $themeImg = '';
                include($theme_url . '/info.php');

                $theme = basename(str_replace('\\', '/', $theme_url));
                $display_theme_name = !empty($themeName) ? $themeName : 'Ramza Theme';
                $theme_profile = !empty($rac_theme_profiles[$display_theme_name]) ? $rac_theme_profiles[$display_theme_name] : array(
                    'summary' => 'A Ramza-compatible theme available in this installation.',
                    'label' => 'Built-in',
                    'locked' => false,
                );
                $display_theme_summary = $theme_profile['summary'];
                $display_theme_label = $theme_profile['label'];
                $display_theme_version = '1.0';
                $theme_image = !empty($themeImg) ? $themeImg : $theme . '/themeLogo.png';
                $theme_image_url = $wo['config']['site_url'] . '/themes/' . $theme_image;
                $is_active_theme = ($theme == $wo['config']['theme']);
                $is_locked_theme = !empty($theme_profile['locked']);
            ?>
            <div class="col-xl-4 col-lg-6 col-md-6">
                <article class="card rac-theme-card <?php echo ($is_active_theme ? 'is-active' : ''); ?> <?php echo ($is_locked_theme ? 'is-locked' : ''); ?>">
                    <div class="card-body">
                        <div class="rac-theme-cover">
                            <img src="<?php echo $theme_image_url; ?>" alt="<?php echo htmlspecialchars($display_theme_name, ENT_QUOTES, 'UTF-8'); ?> preview">
                            <span class="rac-theme-status <?php echo ($is_active_theme ? 'is-active' : ''); ?> <?php echo ($is_locked_theme ? 'is-locked' : ''); ?>">
                                <?php echo ($is_active_theme ? 'Active' : ($is_locked_theme ? 'Coming soon' : 'Ready')); ?>
                            </span>
                        </div>
                        <div class="rac-theme-name">
                            <h5><?php echo htmlspecialchars($display_theme_name, ENT_QUOTES, 'UTF-8'); ?></h5>
                            <span class="badge badge-secondary">v<?php echo htmlspecialchars($display_theme_version, ENT_QUOTES, 'UTF-8'); ?></span>
                            <span class="badge badge-light"><?php echo htmlspecialchars($display_theme_label, ENT_QUOTES, 'UTF-8'); ?></span>
                        </div>
                        <p class="rac-theme-copy"><?php echo htmlspecialchars($display_theme_summary, ENT_QUOTES, 'UTF-8'); ?></p>
                        <div class="rac-theme-meta">
                            <span>Author: Ramza</span>
                            <span>Included theme</span>
                        </div>
                        <div class="rac-theme-actions">
                            <?php if ($is_locked_theme) { ?>
                                <button type="button" class="btn btn-secondary" disabled>
                                    Coming soon
                                </button>
                            <?php } else if ($is_active_theme) { ?>
                                <button type="button" class="btn btn-primary" disabled>
                                    <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" class="mr-1"><path fill="currentColor" d="M10,17L5,12L6.41,10.58L10,14.17L17.59,6.58L19,8M12,2A10,10 0 0,0 2,12A10,10 0 0,0 12,22A10,10 0 0,0 22,12A10,10 0 0,0 12,2Z"></path></svg>
                                    Active theme
                                </button>
                            <?php } else { ?>
                                <a href="#" data-theme="<?php echo htmlspecialchars($theme, ENT_QUOTES, 'UTF-8'); ?>" class="btn btn-primary js-activate-theme">Activate theme</a>
                            <?php } ?>
                        </div>
                    </div>
                </article>
            </div>
            <?php } ?>
        </div>

        <input type="hidden" name="hash_id" value="<?php echo Wo_CreateSession(); ?>">
        <input type="hidden" name="theme" value="" id="theme_name" />
    </form>
    <div class="clearfix"></div>
</div>
<script>
$(function() {
    $(document).on('click', '.js-activate-theme', function(event) {
        event.preventDefault();
        $('#theme_name').val($(this).attr('data-theme'));
        $('form.setting-themes-container').submit();
    });

    $('form.setting-themes-container').ajaxForm({
        url: Wo_Ajax_Requests_File() + '?f=admin_setting&s=updateTheme',
        success: function(data) {
            if (data.status == 200) {
                window.location = "<?php echo Wo_LoadAdminLinkSettings('manage-themes'); ?>";
            }
        }
    });
});
</script>
