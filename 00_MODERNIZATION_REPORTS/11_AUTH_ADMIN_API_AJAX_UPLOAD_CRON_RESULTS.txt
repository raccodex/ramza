RACSocial Phase 1 Runtime - Auth/Admin/API/AJAX/Upload/Cron Gate Results
Generated: 2026-07-11 (Asia/Karachi)

1. Auth Gate
------------
Disposable users:
- phase1user: normal user
- phase1admin: admin user

Tests:
- phase1user login through /requests.php?f=login
- phase1admin login through /requests.php?f=login

Result:
- PASS

Notes:
- Password storage was not redesigned.
- Test users were created only in the disposable database.

2. Admin Gate
-------------
Tests:
- unauthenticated /admincp.php redirects/gates access
- authenticated phase1admin /admincp.php renders with HTTP 200

Result:
- PASS

3. API Gate
-----------
Tests:
- /api.php with missing type returns controlled failed JSON
- /api.php with invalid type returns controlled failed JSON
- /api.php?type=user_data&user=phase1user returns success JSON
- /api.php?type=posts_data&user=phase1user returns controlled JSON
- /api/v2/ direct request does not fatal

Result:
- PASS

4. AJAX Gate
------------
Tests:
- /requests.php?f=login with missing fields returns controlled JSON
- /requests.php?f=resend_two_factor with missing hash returns controlled JSON
- /requests.php?f=session_status while authenticated does not fatal/deprecate

Result:
- PASS

5. Upload Gate
--------------
Test:
- authenticated PNG upload through /requests.php?f=upload-blog-image

Result:
- PASS

Notes:
- The probe generated a valid local 1x1 PNG fixture.
- Filesystem mutation occurred only as part of the isolated runtime test.

6. Cron Gate
------------
Test:
- /cron-job.php executed against the disposable database.

Result:
- PASS

Notes:
- This is a mutating route. It was run only after the disposable database was
  created and configured.
- No cron test was run against the unknown existing database.

END OF GATE RESULTS
